//! Steps 5-6 — long profiles, then surface synthesis.
//!
//! Attempt 4, pipeline step(s) **5-6**. See `docs/network-first/README.md`.
//!
//! **Owns:** node elevations (S proportional to A^-theta, pinned to base level, scaled by the relief budget), then hillslope form hung off the resulting 3-D graph. Divides EMERGE; they are never authored.
//!
//! **Expected failures** (discipline rule 6 — as binding as the targets):
//! all texture and roughness metrics, deliberately. Judged on d2c, relief, descent monotonicity and network agreement only.
//!
//! DEPENDENCY RULE: this crate may not depend on `course-skeleton`,
//! `course-primitives`, `course-amplify` or `course-transforms`. Enforced by
//! `tools/no_old_deps.sh`.
