//! The metric battery. Must stay a pure function of (height, cell, mask) so
//! real and synthetic terrain land in one feature space — that purity is the
//! load-bearing property. See `docs/calibration/metric-battery.md`.
