[package]
name = "course-cli"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Headless pipeline driver: run / batch (the forward-runner) / time"

[dependencies]
course-seed = { workspace = true }
course-spec = { workspace = true }
course-framing = { workspace = true }
course-primitives = { workspace = true }
course-world = { workspace = true }
blake3 = { workspace = true }
