[package]
name = "course-routing"
version.workspace = true
edition.workspace = true
license.workspace = true

# S5 siting + green pool + S6 routing, ported from the Python prototype in
# tools/golf/ (2026-09-14). Consumes the frozen Sandhills terrain (a 2 m
# heightfield and water surface, the creek lines) and emits a Route. Depends
# on the shared substrate only, like course-sandhills.
[dependencies]
course-world = { workspace = true }
rayon = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
