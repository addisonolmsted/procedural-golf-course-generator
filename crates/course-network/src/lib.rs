//! Step 4 — fractal network growth down to ~2 m.
//!
//! Attempt 4, pipeline step(s) **4**. See `docs/network-first/README.md`.
//!
//! **Owns:** the 2-D network graph: headward branching steered by the template's fields, to the corpus Horton ratios.
//!
//! **Expected failures** (discipline rule 6 — as binding as the targets):
//! every SURFACE metric — there is still no surface. Network planform metrics are checkable HERE, and that is the main practical payoff of the reorder.
//!
//! DEPENDENCY RULE: this crate may not depend on `course-skeleton`,
//! `course-primitives`, `course-amplify` or `course-transforms`. Enforced by
//! `tools/no_old_deps.sh`.
