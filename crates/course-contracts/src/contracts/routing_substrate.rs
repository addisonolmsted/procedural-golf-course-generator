//! Contract C2 — the routing substrate: height + exclusion masks + cost fields
//! + metadata + scorer preset. The pipeline's load-bearing interface.
//! Doc: `docs/contracts/C2-routing-substrate.md`.
