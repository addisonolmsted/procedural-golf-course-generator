//! Flow routing over the amplified surface. Must use Barnes-style flat
//! resolution — a D8 kernel without it draws dead-straight axis-aligned
//! channels across every lake and graded flat. Python reference:
//! `tools/macro_campaign/macro_campaign/flow.py`.
