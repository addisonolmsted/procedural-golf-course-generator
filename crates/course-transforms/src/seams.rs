//! Future-biome transform seams. Declared, ordered, and empty — the point is
//! that adding them later is a config change, not a stage rewrite. See
//! `docs/biomes/future-biomes.md`.

pub mod boundary_retreat;
pub mod glacial;
