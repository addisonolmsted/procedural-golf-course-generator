//! terrain-lab — placeholder main; the egui app lands after the primitive
//! library stabilizes (same session, later commit).

fn main() {
    println!("terrain-lab: UI coming in this session — use `cargo run -p \
              golf-landform --example dump_presets` meanwhile");
}
