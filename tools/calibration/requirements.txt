# Continuum calibration harness (offline; not the Cargo workspace).
# Reuses the metrics package (tools/metrics) + its deps.
numpy>=1.24
scipy>=1.10
pandas>=2.0
pyarrow>=14
scikit-learn>=1.3
SALib>=1.4
matplotlib>=3.7
PyYAML>=6.0
